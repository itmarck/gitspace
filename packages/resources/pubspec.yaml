name: resources
description: Resource functions
version: 1.0.0

publish_to: 'none'

environment:
  sdk: '>=2.16.0 <3.0.0'

dependencies:
  domain:
    path: ../domain
  flutter_secure_storage: ^5.0.2
  http: ^0.13.4

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
