import 'package:domain/domain.dart';
import 'package:types/document.dart';

import 'src/mapper.dart';

class GitlabProjectMapper extends Mapper<Project> {
  @override
  Project from(Document document) {
    throw UnimplementedError();
  }

  @override
  Document to(Project object) {
    throw UnimplementedError();
  }
}

class GithubProjectMapper extends Mapper<Project> {
  @override
  Project from(Document document) {
    throw UnimplementedError();
  }

  @override
  Document to(Project object) {
    throw UnimplementedError();
  }
}
